#!/usr/bin/env nu

try { plugin add ~/.cargo/bin/nu_plugin_gstat }
# try { plugin add ~/.cargo/bin/nu_plugin_periodic_table }
# try { plugin add ~/.cargo/bin/nu_plugin_net }
try { plugin add ~/.cargo/bin/nu_plugin_query }
try { plugin add ~/.cargo/bin/nu_plugin_polars }
try { plugin add ~/.cargo/bin/nu_plugin_explore_ir }
